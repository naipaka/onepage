name: onepage
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter

dev_dependencies:
  altive_lints: ^1.9.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
