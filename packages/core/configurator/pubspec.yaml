name: configurator
description: A Flutter package for Firebase Remote Config wrapper functionality.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  firebase_remote_config: ^6.5.3
  flutter:
    sdk: flutter
  meta: ^1.17.0

dev_dependencies:
  # Temporarily disabled: analyzer constraint conflicts with the riverpod stack (analyzer 12).
  # Replaced by an in-repo lint package. See https://github.com/naipaka/onepage/issues/249
  # altive_lints: ^2.3.0
  build_runner: ^2.15.0
  firebase_core: ^4.10.0
  firebase_core_platform_interface: ^7.0.1
  firebase_remote_config_platform_interface: ^3.0.2
  flutter_test:
    sdk: flutter
  mockito: ^5.6.4
  plugin_platform_interface: ^2.1.8
