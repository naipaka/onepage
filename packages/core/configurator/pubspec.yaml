name: configurator
description: A Flutter package for Firebase Remote Config wrapper functionality.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  firebase_remote_config: ^6.1.2
  flutter:
    sdk: flutter
  meta: ^1.17.0

dev_dependencies:
  altive_lints: ^1.25.0
  build_runner: ^2.13.1
  custom_lint: ^0.8.1
  firebase_core: ^4.2.1
  firebase_core_platform_interface: ^6.0.2
  firebase_remote_config_platform_interface: ^2.1.2
  flutter_test:
    sdk: flutter
  mockito: ^5.6.4
  plugin_platform_interface: ^2.1.8
