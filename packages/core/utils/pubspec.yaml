name: utils
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  simple_logger: ^1.10.0

dev_dependencies:
  altive_lints: ^1.18.0
  custom_lint: ^0.7.3
  # Because flutter_test depends on fake_async 1.3.1.
  fake_async: ^1.3.1
  # Because flutter_test depends on test_api 0.7.3.
  test: ^1.25.8
