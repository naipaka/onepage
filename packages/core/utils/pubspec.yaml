name: utils
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.7.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  simple_logger: ^1.10.0

dev_dependencies:
  altive_lints: ^1.19.1
  custom_lint: ^0.7.5
  # Because flutter_test depends on fake_async 1.3.2.
  fake_async: ^1.3.2
  test: ^1.25.15
