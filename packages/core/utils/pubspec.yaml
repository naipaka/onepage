name: utils
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  simple_logger: ^1.10.0

dev_dependencies:
  altive_lints: ^1.16.0
  custom_lint: ^0.7.0
  fake_async: ^1.3.2
  test: ^1.25.9
