name: utils
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.2
  simple_logger: ^1.10.0

dev_dependencies:
  altive_lints: ^1.21.0
  custom_lint: ^0.7.5
  # Because flutter_test depends on fake_async 1.3.3.
  # https://github.com/flutter/flutter/blob/stable/packages/flutter_test/pubspec.yaml#L25
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter
  mockito: ^5.4.6
