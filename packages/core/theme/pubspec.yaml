name: theme
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  altive_lints: ^4.1.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
