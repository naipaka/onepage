name: notification_client
description: A package for handling local notifications in the diary app.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  clock: ^1.1.2
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.3.0
  flutter_timezone: ^4.1.1
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  prefs_client:
    path: ../prefs_client
  timezone: ^0.10.1

dev_dependencies:
  altive_lints: ^1.21.0
  build_runner: ^2.5.4
  custom_lint: ^0.7.5
  flutter_test:
    sdk: flutter
  freezed: ^3.0.6
  json_serializable: ^6.9.5
  mockito: ^5.4.6
