name: haptics
description: A package that provides haptic feedback functionality with preference-based control.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  prefs_client:
    path: ../../core/prefs_client

dev_dependencies:
  altive_lints: ^4.1.0
  build_runner: ^2.16.1
  flutter_test:
    sdk: flutter
  mockito: ^5.8.1
