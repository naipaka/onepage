name: diary
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  # Because flutter_test depends on clock 1.1.1.
  clock: ^1.1.1
  db_client:
    path: ../../core/db_client/
  flutter:
    sdk: flutter
  utils:
    path: ../../core/utils/

dev_dependencies:
  altive_lints: ^1.18.0
  build_runner: ^2.4.15
  custom_lint: ^0.7.0
  mockito: ^5.4.5
  # Because flutter_test depends on test_api 0.7.3.
  test: ^1.25.8
