name: in_app_reviewer
description: "In-app review prompting functionality for One Page diary app."
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  clock: ^1.1.2
  flutter:
    sdk: flutter
  in_app_review: ^2.0.12
  prefs_client:
    path: ../../core/prefs_client

dev_dependencies:
  # Temporarily disabled: analyzer constraint conflicts with the riverpod stack (analyzer 12).
  # Replaced by an in-repo lint package. See https://github.com/naipaka/onepage/issues/249
  # altive_lints: ^2.3.0
  build_runner: ^2.14.1
  flutter_test:
    sdk: flutter
  mockito: ^5.6.4
