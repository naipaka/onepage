name: backup
description: "Backup feature package"
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.7.2

resolution: workspace

dependencies:
  clock: ^1.1.2
  db_client:
    path: ../../core/db_client/
  file_picker: ^10.0.0
  flutter:
    sdk: flutter
  gap: ^3.0.1
  # Because flutter depends on intl 0.19.0.
  # https://github.com/flutter/flutter/blob/stable/packages/flutter/pubspec.yaml#
  intl: ^0.19.0
  package_info_plus: ^8.3.0
  path: ^1.9.1
  path_provider: ^2.1.5
  share_plus: ^11.0.0

dev_dependencies:
  altive_lints: ^1.19.1
  build_runner: ^2.4.15
  custom_lint: ^0.7.5
  flutter_test:
    sdk: flutter
  mockito: ^5.4.5
