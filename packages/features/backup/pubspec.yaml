name: backup
description: "Backup feature package"
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  clock: ^1.1.2
  db_client:
    path: ../../core/db_client/
  file_picker: ^10.2.0
  flutter:
    sdk: flutter
  gap: ^3.0.1
  # Because flutter depends on intl 0.20.2.
  # https://github.com/flutter/flutter/blob/stable/packages/flutter/pubspec.yaml#L42
  intl: ^0.20.2
  package_info_plus: ^8.3.0
  path: ^1.9.1
  path_provider: ^2.1.5
  share_plus: ^11.0.0

dev_dependencies:
  altive_lints: ^1.22.1
  build_runner: ^2.6.0
  custom_lint: ^0.8.0
  flutter_test:
    sdk: flutter
  mockito: ^5.5.0
