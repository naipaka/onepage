name: backup
description: "Backup feature package"
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  clock: ^1.1.2
  db_client:
    path: ../../core/db_client/
  file_picker: ^8.1.7
  flutter:
    sdk: flutter
  gap: ^3.0.1
  intl: ^0.19.0
  package_info_plus: ^8.1.3
  path: ^1.9.0
  path_provider: ^2.1.5
  share_plus: ^10.1.4

dev_dependencies:
  altive_lints: ^1.18.0
  build_runner: ^2.4.14
  custom_lint: ^0.7.3
  flutter_test:
    sdk: flutter
  mockito: ^5.4.5
